f=int(input("Please enter a number:"))
def factorial(f):
    return(range(f,1,-1))
nums=(list(factorial(f)))
def fact(b):
    total=1
    for i in b:
        total=total*i
    return total
print (fact(nums))
